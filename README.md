# string-encryptor
Encrypt/decrypt strings in a browser using AES-256 GCM

### Operation

Start with `./up.sh`, navigate to [http://localhost:12345](http://localhost:12345), shutdown with `./down.sh`
